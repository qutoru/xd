"""Application / orchestration layer.

Holds the CLI and command orchestration that used to live in ``main.py`` (now a
thin bootstrap). Reuses Production Core and the platform integration pipeline;
contains no strategy or research logic of its own.
"""
